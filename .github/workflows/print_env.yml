name: Print Env
on: workflow_dispatch
jobs: 
  print_enviroment:
    environment: testing
    runs-on: ubuntu-latest
    steps:
      - name: Print environment secret
        run: echo "enviroment ${{ vars.MY_VARIABLE }}"
